import { defineConfig } from "oxfmt";

export default defineConfig({
    "printWidth": 110,
    "tabWidth": 4,
    "trailingComma": "all",
    "quoteProps": "preserve",
    "arrowParens": "avoid",
});
